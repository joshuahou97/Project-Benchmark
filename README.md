# SQL Agent Benchmark

This project evaluates the performance of LLM-based SQL agents using a controlled benchmark.

The benchmark includes:

- A synthetic dataset
- A LangChain-based SQL agent
- A set of evaluation queries
- Benchmark metrics including:
  - Accuracy


The goal is to analyze how reliably an LLM can translate natural language questions into SQL queries.